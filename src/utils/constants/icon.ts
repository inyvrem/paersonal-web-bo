import {
  IconDefinition,
  faStore,
  faCity,
} from "@fortawesome/free-solid-svg-icons"

export namespace Icon {
  export const HOME: IconDefinition = faStore
  export const PAGES: IconDefinition = faCity
}
