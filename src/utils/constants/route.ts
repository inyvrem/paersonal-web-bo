export namespace Route {
  export const HOME: string = "/"
  export const SIGN_IN: string = "/sign-in"

  // Side bar
  export const PAGES: string = "/pages/"
  export const SECTIONS: string = "/sections"
  export const NEW_SECTION: string = SECTIONS + "/new"
}
